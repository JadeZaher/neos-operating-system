[build]
buildCommand = ""
watchPatterns = ["neos-operating-system/agent/**"]

[deploy]
startCommand = "python -m neos_agent.main --port $PORT"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
